<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timerpart1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/official_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/official_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CatchResultActivity"></activity>
        <activity android:name=".CatchStartActivity" />
        <activity android:name=".FirstGameCatchActivity" />
        <activity android:name=".GameChoicesActivity" />
        <activity android:name=".ListTransitionActivity" />
        <activity
            android:name=".ItemActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DashboardActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TimerActivity" />
        <activity android:name=".IntroActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BreakActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".PomodoroActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>